# Database connection (used by API via Prisma)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/payday_poc?schema=public"

# Redis connection (not yet integrated)
REDIS_URL="redis://localhost:6379"

# API server port (used by NestJS)
PORT=3001

# Frontend API base URL (used by Next.js, must have NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_API_BASE_URL="http://localhost:3001"
